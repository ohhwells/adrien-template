// Legacy file kept for backwards-compatibility; canonical content now lives
// in `consultations.ts`. This re-export prevents stale imports from breaking.
export { consultationsList as servicesContent } from './consultations';
